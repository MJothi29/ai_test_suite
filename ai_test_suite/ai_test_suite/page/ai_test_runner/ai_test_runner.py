# Server-side controller for the AI Test Runner desk page.
# The page itself is rendered client-side (see ai_test_runner.js); this file
# only needs to exist for Frappe's page loader / asset bundling.
